/**
 * Same horizontal padding & width as Popular Routes / Top Picks:
 * - maxWidth xl (~1536px)
 * - disableGutters so px fully controls inset
 * - xs: theme spacing 2 → 16px, md: 10 → 80px (default MUI theme)
 */
export const SECTION_MAX_WIDTH = 'xl';

export const SECTION_CONTAINER_SX = { px: { xs: 2, md: 10 } };
